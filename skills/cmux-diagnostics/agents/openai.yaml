interface:
  display_name: "amux Diagnostics"
  short_description: "Check amux hooks, restore, CLI, and settings."
  default_prompt: "Use $cmux-diagnostics to collect a read-only amux health report and explain any hook or session restore problems."
