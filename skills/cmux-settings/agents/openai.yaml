interface:
  display_name: "cmux Settings"
  short_description: "Inspect, edit, validate, and reload amux.json settings."
  default_prompt: "Use this skill to change amux settings safely: map user-facing preferences to amux.json paths, edit with the bundled helper, validate recognized keys, and rely on cmux's live config reload."
