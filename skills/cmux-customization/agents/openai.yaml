interface:
  display_name: "cmux Customization"
  short_description: "Customize cmux actions, UI, layouts, and presets."
  default_prompt: "Use $cmux-customization to pick an examples-library preset, wire it into amux.json, and validate the cmux config."
